class Solution:
    def multiply(self, num1: str, num2: str) -> str:
        if not num1.isdigit() or not num2.isdigit():
            return "Invalid input"
        
        x = int(num1)
        y = int(num2)
        result = x * y
        return str(result)
